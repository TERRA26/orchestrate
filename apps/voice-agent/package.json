{
  "name": "@orchestrate/voice-agent",
  "version": "0.0.15",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit",
    "dev": "node --env-file=../../.env.local --import tsx src/main.ts dev",
    "start": "node --env-file=../../.env.local --import tsx src/main.ts start",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@livekit/agents": "^1.4.0",
    "@livekit/agents-plugin-openai": "^1.4.0",
    "@livekit/rtc-node": "^0.13.27",
    "@orchestrate/shared": "workspace:*",
    "zod": "^4.1.8"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "^4.21.0",
    "typescript": "catalog:",
    "vitest": "catalog:"
  },
  "engines": {
    "node": "^22.16 || ^23.11 || >=24.10"
  }
}

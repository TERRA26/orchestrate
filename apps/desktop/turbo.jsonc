{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist-electron/**"],
    },
    "dev": {
      "dependsOn": ["orchestrate#build"],
      "persistent": true,
    },
    "start": {
      "dependsOn": ["build", "@orchestrate/web#build", "orchestrate#build"],
      "cache": false,
      "persistent": true,
    },
    "smoke-test": {
      "dependsOn": ["build", "@orchestrate/web#build", "orchestrate#build"],
      "cache": false,
      "outputs": [],
    },
  },
}
